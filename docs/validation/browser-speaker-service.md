# 浏览器扬声器服务边界验证

本次执行 `tests/Phyphox.BrowserSpeaker.Tests`，构建 0 警告、0 错误，聚焦验证通过：

- 后台 AudioMixer 从实验 XML 的 tone 输出产生实际 PCM：48 kHz、100 ms、双声道，1 kHz、振幅 0.5，左右声道一致。
- 未启动和暂停时不发送可播放样本；恢复保留同一 streamId；停止后旧 streamId 失效。
- PCM 请求序号不可重复。
- 前端 TypeScript 检查通过。

未创建浏览器 AudioContext 播放测试声音，未打开真实扬声器，未验证声压、实际播放延迟、声卡采样率、长期无断续播放或音频测量精度。上述通过结果为服务协议与数值验证，不是真实音频设备验收。

实现范围：用户明确点击启用浏览器扬声器；初始音量 10%；服务产生立体声 PCM，浏览器只排程播放和音量控制。每次响应最多 100 ms，单请求在途；后台生成前瞻预算 200 ms，客户端排程约 220 ms 上限。浏览器失联 1.5 秒立即关闭本地声音，后台超过 3 秒无请求停止实验。暂停保留配置并停止已排程声音；停止、清空和切换实验使流失效。隐藏整个浏览器页面、关闭或卸载组件时停止本地声音并请求停止实验；仅应用内部切页保持组件挂载不触发此行为。

目前使用浏览器默认系统输出设备，未实现指定 sinkId。仅已实现的循环音频支持迁移；非循环逐分析触发仍阻断。动态声源参数变化重建现有 AudioMixer，不提供跨参数更新的相位连续保证。浏览器 AudioContext 对设备混音率的适配由浏览器负责，实验 PCM 响应始终标注后台源采样率。
